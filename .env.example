# MCP HTTP Bridge 配置
# 复制此文件为.env并填写配置

# 服务端口
PORT=3000

# Shopify MCP 端点配置
# 商店前台 MCP (公开数据，如商品搜索)
MCP_STOREFRONT_ENDPOINT=https://your-shop.myshopify.com/api/mcp

# 客户账户 MCP (需要认证，如订单查询)
MCP_CUSTOMER_ENDPOINT=https://your-shop.account.myshopify.com/customer/api/mcp

# 静态访问令牌 (可选，用于需要认证的MCP端点，如果不使用OAuth流程)
MCP_ACCESS_TOKEN=

# ============================================
# Shopify OAuth 配置 (用于自动获取客户 token)
# ============================================

# Shopify App Client ID(从Shopify Partners Dashboard获取)
SHOPIFY_CLIENT_ID=your_client_id_here

# 商店ID(如your-store.myshopify.com)
SHOPIFY_SHOP_ID=your-store.myshopify.com

# OAuth回调URL(必须与Shopify App配置中的redirect_uri一致)
OAUTH_REDIRECT_URI=http://localhost:3000/auth/callback

# 客户API权限范围(逗号分隔)
SHOPIFY_CUSTOMER_SCOPES=customer_read_customers,customer_read_orders

